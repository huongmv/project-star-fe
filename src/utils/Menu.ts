import { apiPost, apiGetNoParam } from '@/api/index'
const menus = { 
    getRouter(key: any, value: any) {
        // let rouList = '';
        // apiGetNoParam(CONTRACT_GET_ALL_BY_ID + buildId).then(res => {
        //     let response: any = res
        //     tableData.value = response.data.data
        // })
    },
}
export default menus